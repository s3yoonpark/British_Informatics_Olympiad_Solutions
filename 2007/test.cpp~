#include <bits/stdc++.h> 

using namespace std; 

int main() {
	for (int i = 5; i >= 0; --i) {
		if ((1 << i) & 2 == (1 << i)) {
			cout << 1; 
		} else {
			cout << 0; 
		}
	}
	cout << "\n";

	cout << "\n"; 
	cout << (1<<1) << "\n";
	cout << (((1<<1) & 2) == (1<<1)) << "\n"; 
	return 0; 
}
